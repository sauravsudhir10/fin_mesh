[project]
name = "fin-mesh"
version = "0.1.0"
description = "Data Mesh on AWS Lake Formation"
requires-python = ">=3.10"
dependencies = [
    "awscli>=1.27.0",
]

[tool.uv]
required-version = "0.10"

